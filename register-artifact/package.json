{
  "name": "register-artifact",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --quiet"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3"
  }
}
